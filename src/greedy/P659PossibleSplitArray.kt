package greedy

/**
 * 分割数组为连续子序列
 */
object P659PossibleSplitArray {
    fun isPossible(nums: IntArray): Boolean {
        if (nums.size < 3) {
            return false
        }

        for (num in nums) {

        }
        TODO("")
    }
}